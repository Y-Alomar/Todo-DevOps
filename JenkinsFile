pipeline {
    agent any
    
    environment {
        DOCKER_HUB_REPO = 'yousefca'
        AZURE_VM_IP = '172.179.173.132'              
        AZURE_VM_USER = 'azureuser'
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo '📦 Pulling code from GitHub...'
                checkout scm
            }
        }
        
        stage('Build Docker Images') {
            steps {
                echo '🔨 Building Docker images...'
                bat 'docker-compose build'
            }
        }
        
        stage('Login to Docker Hub') {
            steps {
                echo '🔐 Logging into Docker Hub...'
                script {
                    withCredentials([
                        string(credentialsId: 'dockerhub-username', variable: 'DOCKER_USER'),
                        string(credentialsId: 'dockerhub-token', variable: 'DOCKER_PASS')
                    ]) {
                        bat 'docker login -u %DOCKER_USER% -p %DOCKER_PASS%'
                    }
                }
            }
        }
        
        stage('Tag and Push Images') {
            steps {
                echo '🏷️ Tagging and pushing images...'
                bat """
                    docker tag my-todo-app-backend:latest %DOCKER_HUB_REPO%/todo-backend:latest
                    docker tag my-todo-app-frontend:latest %DOCKER_HUB_REPO%/todo-frontend:latest
                    docker push %DOCKER_HUB_REPO%/todo-backend:latest
                    docker push %DOCKER_HUB_REPO%/todo-frontend:latest
                """
            }
        }
        
        stage('Deploy to Azure VM') {
            steps {
                echo '🚀 Deploying to Azure VM...'
                script {
                    withCredentials([sshUserPrivateKey(
                        credentialsId: 'azure-vm-ssh',
                        keyFileVariable: 'SSH_KEY'
                    )]) {
                        // Fix permissions using SYSTEM account (Jenkins runs as SYSTEM)
                        bat """
                            icacls "%SSH_KEY%" /reset
                            icacls "%SSH_KEY%" /inheritance:r
                            icacls "%SSH_KEY%" /grant:r "NT AUTHORITY\\SYSTEM:(R)"
                            icacls "%SSH_KEY%" /grant:r "%USERDOMAIN%\\%USERNAME%:(R)"
                        """
                        
                        // Now SSH
                        bat """
                            ssh -i "%SSH_KEY%" -o StrictHostKeyChecking=no %AZURE_VM_USER%@%AZURE_VM_IP% "cd todo-app && git pull origin main && docker-compose pull && docker-compose up -d --remove-orphans"
                        """
                    }
                }
            }
        }
        
        stage('Verify Deployment') {
            steps {
                echo '✅ Verifying deployment...'
                script {
                    withCredentials([sshUserPrivateKey(
                        credentialsId: 'azure-vm-ssh',
                        keyFileVariable: 'SSH_KEY'
                    )]) {
                        // Fix permissions again
                        bat """
                            icacls "%SSH_KEY%" /reset
                            icacls "%SSH_KEY%" /inheritance:r
                            icacls "%SSH_KEY%" /grant:r "NT AUTHORITY\\SYSTEM:(R)"
                            icacls "%SSH_KEY%" /grant:r "%USERDOMAIN%\\%USERNAME%:(R)"
                        """
                        
                        bat """
                            ssh -i "%SSH_KEY%" -o StrictHostKeyChecking=no %AZURE_VM_USER%@%AZURE_VM_IP% "docker ps"
                        """
                    }
                }
            }
        }
    }
    
    post {
        success {
            echo '✅ ========================================='
            echo '✅  DEPLOYMENT SUCCESSFUL! 🎉'
            echo '✅ ========================================='
            echo '✅ Docker Hub: https://hub.docker.com/u/%DOCKER_HUB_REPO%'
            echo '✅ Live App: http://%AZURE_VM_IP%'
            echo '✅ ========================================='
        }
        failure {
            echo '❌ Deployment failed! Check logs above.'
        }
        always {
            bat 'docker logout || exit 0'
        }
    }
}